{
  "name": "@acme/storefront",
  "version": "3.4.1",
  "private": true,
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "rebuild": "rimraf dist && vite build"
  },
  "dependencies": {
    "vite": "^6.0.0"
  },
  "pnpm": {
    "overrides": {
      "semver@<7.5.2": ">=7.5.2"
    },
    "peerDependencyRules": {
      "ignoreMissing": ["react"]
    },
    "onlyBuiltDependencies": ["sharp"]
  }
}
