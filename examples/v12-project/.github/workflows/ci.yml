name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: pnpm install --frozen-lockfile
      - name: Report peer dependency issues
        run: pnpm install --resolution-only
      - run: pnpm test
